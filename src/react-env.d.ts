/// <reference types="react" />
/// <reference types="react-dom" />

// Explicit React type references keep JSX intrinsic elements available in
// strict Cloudflare/CI TypeScript builds, even when the compiler's ambient
// type discovery differs from the local development environment.
export {};
